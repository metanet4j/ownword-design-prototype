# Ownword v0.2.0 原型交付

## 范围与入口

本版为文字内容高保真交互原型，沿用 React Spectrum S2；Markdown 编辑参考工作区 `reference/xLog-dev` 的编辑器、工具栏、双栏滚动和渲染源码。

预览：<http://127.0.0.1:4312/#/content>。启动、状态与依赖由上级任务 harness 维护，运行 `../init.sh --serve`。旧版 4311 保留。

权威设计：工作区 `_task/system-design/spec/prd/v0.2.0/设计文档v0.2.0-20260917-210556.md`。本文记录交付与证据，不重新定义产品事实。

## 页面与操作

| 入口 | 可操作内容 |
| --- | --- |
| 我的内容 | 当前身份草稿、已发布内容、筛选、新建、删除确认、空态与失败重试 |
| 写作 | CodeMirror Markdown、格式工具、预览、双栏同步、自动保存、离开保护、文件导入导出 |
| 检查发布 | 作者与完整 BAP ID、最新正文快照、公开影响、返回修改 |
| 签名与发布 | 模拟授权、签名、发布、取消、失败、待核实查询与恢复 |
| 阅读 | 访客阅读、章节目录、复制链接、导出原文、创建或继续修订 |
| 版本历史 | 阅读各版、返回最新版本、保留旧正文和旧链接 |
| 签名与链上记录 | 展开 Proof，分别查看签名样例、确认状态、作者及完整 TxID |

页脚“交互原型”可选择内容列表、存储、发布、查询、确认、账户变化及 Proof 场景。场景有明确结果，不使用随机失败。页头可切换中英与浅深主题。

## 验证映射

每项过程、截图及 JSON 证据在 `evidence/C编号/`，逐项说明见各目录 `verification.json`。

| 项目 | 本轮证据 |
| --- | --- |
| C01 | 工作台筛选、空态、失败与身份门槛 |
| C02 | 中文正文输入、选区格式与撤销、查找、手机切换及位置保持 |
| C03 | 支持语法、安全文字边界、滚动同步、宽表格不撑开页面 |
| C04 | 保存后刷新恢复、写入失败保护、身份隔离、内存恢复、确认删除 |
| C05 | 实际 UTF-8 中文文件上传下载；BOM 移除，CRLF 与正文逐字节保留；取消与错误不覆盖 |
| C06 | 最新正文与作者快照、完整 BAP ID、空白及字节上限门槛 |
| C07 | 签名与发布分阶段、取消与失败保留、未知结果查询、刷新恢复、持久化失败与账户变化 |
| C08 | 访客阅读、未知标识提示、复制反馈、原文下载、本机发布内容重开 |
| C09 | 新旧版本独立、历史往返、无修改及改回原文门槛、修订草稿复用 |
| C10 | Proof 三态、独立确认、完整 TxID、复制失败、被改动样例不显示有效 |
| X01 / X02 | `evidence/C10/flow-*.json` 比对同源渲染与 Review 中英/主题保持；320px 边界和弹窗焦点另有记录 |

环境检查还覆盖 92 条既有身份模型断言、内容状态不变量、Markdown 处理、351 项双语文案键、JSX 语法及 68 个设计 token 引用。harness 结构评分不代表产品集成验收。

内容详情目录的定向验证见 `evidence/R02/verification.json`，可运行检查为 `evidence/R02/check-toc.js`。

审查修复和相关回归见[P0审查与修复v0.2.0-20260917-235527](P0审查与修复v0.2.0-20260917-235527.md)；验收行为以权威 PRD 为准。

## 可复现与限制

- 所有依赖加载自本地资源，编辑器及解析依赖有锁文件；重新构建使用 `npm ci`、`npm run build`。日常启动不重复安装依赖。
- 草稿按 BAP ID 保存在当前浏览器。刷新需重新连接身份，未保存正文受原生离开提醒保护。
- 模拟发布记录与操作快照保存在当前 origin。内置公开样例链接可复现；新演示内容链接在其他浏览器中可能不存在。
- 未连接真实钱包、签名、费用、交易或链上查询；Proof 为可控样例，未执行密码学验证。
- 浏览器验证使用宿主 Chromium 与 `agent-browser`。已输入中文，但未执行操作系统级中文输入法组合测试；未做 Safari、实体手机或全量旧版回归。
- 功能实现与定向验证完成后状态为 `needs-review`。用户视觉复核通过后再标记 `done`。

## 维护位置

`content-model.js` 管理演示数据与本地记录；`content.jsx` 组织路由、草稿与确认；`content-editor.jsx` 和 `markdown-tools.mjs` 共用正文渲染；`content-publication.jsx` 表达模拟发布流程；`content-reader.jsx` 提供阅读、历史和 Proof。界面文案统一在 `copy.js`，样式在 `content.css`，S2 绑定见 `_d_meta.json`。
