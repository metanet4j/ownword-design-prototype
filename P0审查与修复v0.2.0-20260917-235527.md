# v0.2.0 P0 审查与修复

审查基线：`68b6d4c`。范围：草稿持久化、身份隔离、模拟签名发布与恢复。本轮确认 1 项 P0、2 项关联 P1，均已修复并定向验证；等待用户复核。

P0 指原型正常使用即可造成已保存数据静默丢失；P1 指需故障恢复等前提，或数据尚在但关键反馈错误。此分级不代表生产钱包或链上安全审计。

## 发现与修复

| 编号 | 复现与影响 | 当前处理 | 提交 |
| --- | --- | --- | --- |
| P0-01 | 两个标签页先后打开工作区；第一页保存新稿，第二页再保存。两页均显示已保存，但第二页整批旧快照覆盖首页新稿。发布日志存在相同覆盖路径。 | 同一 origin 只允许一个页面写入；其他页面保留公开阅读。关闭原页后重试，接手前重读最新草稿和发布记录。 | `36eeacb` |
| P1-01 | 本地读取失败时继续编辑，外部切换身份，恢复存储后在工作台重试。重载数据清空原身份未保存的正文。 | 保留磁盘原稿，将不同的内存正文保留为新 ID 的恢复草稿；沿用原身份，落盘前保持未保存状态。编辑页也可直接重试恢复。 | `66f1dfd` |
| P1-02 | 从草稿列表重新打开待核实操作，查询已接收结果。草稿移除后页面误显示“未找到此内容”，实际记录已保存。 | 操作完成后继续展示发布结果和阅读入口；再次打开原草稿链接也能查看结果。 | `a2cbce6` |

页面互斥使用浏览器原生 Web Locks；同源页面可持有独占锁，关闭后释放，符合[官方规范的文档编辑场景](https://w3c.github.io/web-locks/#motivating-use-cases)。该保护由原型页面共同遵守，不替代生产存储事务。

## 复现与证据

证据目录：`evidence/R01/`。在独立测试浏览器中验证，存储故障通过测试数据注入；未修改用户真实草稿。

| 验收引用 | 实测结果 | 证据 |
| --- | --- | --- |
| C04-E | 旧版双页保存静默覆盖；修复后第二页无新建入口，重试仍受保护，公开阅读可用 | `two-tabs-before.json`、`two-tabs-protected.json`、`second-tab-reading.json` |
| C04-E、C07-A | 首页面模拟发布后关闭，旧快照页面接手加载新记录；新建草稿保存后旧发布仍在 | `takeover-after.json`、`takeover-write-after.json` |
| C04-D/F | 重试前未保存正文丢失；修复后磁盘原稿和恢复草稿均在，其他身份看不到正文，打开后成功保存 | `recovery-source-before.json`、`recovery-lost-before.json`、`recovery-owner-after.json`、`recovery-both-after.json`、`recovery-durable-after.json` |
| C04-F | 编辑页直接重试保留当前文字和原稿 | `editor-recovery-after.json` |
| C07-D/E/F | 待核实操作刷新后保留；切换身份不能查询旧操作；切回原身份后可恢复 | `unknown-before-reload.json`、`unknown-owner-check.json` |
| C07-A/D | 同一按钮连续触发两次查询，只生成一份发布记录，旧发布仍在；结果显示成功及独立确认状态，阅读入口正常 | `query-idempotent-after.json`、`query-result-after.json` |
| 响应式 | 英文桌面、中文 390px 页面占用提示可读，手机无横向溢出 | `second-tab-protected.png`、`second-tab-protected-mobile.png`、`mobile-check.json` |
| 视觉与日志 | 已查看恢复草稿和查询结果截图；运行错误文件为空，故障注入产生的保存失败日志符合预期 | `recovered-draft.png`、`query-result-after.png`、`final-browser-errors.txt`、`final-browser-console.txt` |

`../init.sh` 全部通过：身份模型、内容模型、Markdown、JSX、348 项双语文案键、68 个设计 token 引用与 harness。新增模型断言覆盖恢复时原稿保留、两个作者、新稿、CRLF 原文、未保存状态、已落盘同内容不重复建稿及拒绝读取失败的快照。输出见 `evidence/R01/harness-check.txt`。

手工复核从两个已刷新的标签页打开 `http://127.0.0.1:4312/#/content`，连接同一示例身份。首页写入，第二页应提示原页已打开；关闭首页，在第二页重试，应看到最新内容。恢复场景及查询结果按上表触发，源码根因分别位于 `content.jsx` 的写入门禁、`content-model.js` 的 `recoverDrafts` 和 `content.jsx` 的发布结果分支。

## 边界与交付

- 更新后应刷新所有原型标签页；仍运行旧脚本的页面不参与互斥保护。刷新前先保存或导出尚未保存的文字。
- 仅在宿主 Chromium、localhost 验证；未验证 Safari、实体手机和不支持 Web Locks 的浏览器。原型在无法获得可靠页面互斥时保持只读。
- 工作台保留的恢复草稿仍需打开并确认已保存；在落盘前关闭整个浏览器无法保证恢复，页面保留原生未保存提醒。
- 签名、发布和 Proof 均为本地样例；未连接真实钱包、网络交易或数据库。此次未执行全量旧版浏览器回归。
- 原型保持 `prototype/v0.2.0-content` 分支，状态 `needs-review`，尚未合并或推送。
