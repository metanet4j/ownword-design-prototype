# 原型演练

原型用于表达产品页面、交互与状态；沿用本地 React/Babel、S2 和模拟数据。业务规则引用[核心认知](../../_task/system-design/spec/核心认知.md)，版本范围引用[PRD v0.1](../../_task/system-design/spec/prd/v0.1/设计文档v0.1.md)。

## 操作方式

- 从页脚“交互原型”打开设置；关闭后，页面只保留产品操作。
- 身份场景决定下一次连接或重新解析的结果。
- “下一次操作结果”决定批准连接、创建或更新后的结果，只生效一次。取消授权保留配置；重试默认成功。
- “下一次账户变化”可选授权时或处理中切换账户／断开。创建身份或更新资料进入对应阶段后自动触发一次：授权窗口打开 1.2 秒后，处理开始 0.6 秒后。取消或离开阶段会清除计时器，未触发的配置仍保留。
- 设置内可立即切换示例账户、断开、重新解析或重置会话。重置会清除待触发的结果、账户事件和复制失败开关。
- 顶栏断开连接、产品失败后的重试及钱包授权中的批准／取消继续保留。
- 示例数据和模拟行为说明统一放在原型设置，身份页与卡片不再重复展示。授权弹窗顶部标注“Yours Wallet · 外部钱包授权”；未保存修改确认、钱包不可用等应用提示仍标注 OWNWORD。

## 验证

窄屏页头收紧按钮左右留白，保留至少 44px 点击高度和全部文字。320×568 已连接页头为两行、120px，未连接时为一行、68px；桌面布局不变。窄屏专项 40 条通过，覆盖中英×浅深、320/390/650/768/1440px、无溢出或重叠、滚动吸顶、偏好面板键盘开关、身份导航和断开操作。4 次页头 axe 审计无 violation/incomplete；运行错误为空。证据见 `evidence/narrow-header-*`，具体尺寸见 `narrow-header-geometry.json`。

公开身份卡下方提供“查看链上记录”按钮，默认收起。点击或 Enter／空格可展开与收起；展开后 Tab 可到达完整 TxID 复制按钮，收起后内容退出键盘顺序。与卡片背面共用同一记录展示组件，不改变旋转和拖动效果。

导航在页面滚动时保持顶部可见。全局错误、成功和取消提示显示在底部，6 秒后消失；悬停或键盘焦点停留时暂停，离开后重新计时，也可手动关闭。提示消失不清除失败状态或草稿，主操作仍可重试。字段校验保留在字段旁，解析失败保留完整错误页面。

矮屏表单中的提示位于固定操作条上方；页脚保留足够滚动空间，原型入口不会被操作条遮挡。

启动预览后，在原型目录运行：

```bash
node check-model.cjs
node check-copy.cjs
python3 check-tokens.py
OWNWORD_PORT=4311 python3 check-browser.py --controls-only
OWNWORD_PORT=4311 python3 check-browser.py --feedback-only
OWNWORD_PORT=4311 python3 check-browser.py --chain-only
OWNWORD_PORT=4311 python3 check-browser.py --header-only
```

浏览器检查需在宿主环境执行。场景控制专项基线 45 条通过，覆盖连接取消、创建与更新失败、一次性重试、四类账户事件、旧结果隔离、设置收起、示例说明集中及外部钱包边界。该基线包含中英×浅深的 320×568 检查和 8 次 axe 审计。

当前导航与提示专项 29 条通过，覆盖双语双主题、滚动吸顶、提示底部定位、关闭与自动消失、悬停与键盘暂停、草稿及重试保留、矮屏操作条避让。证据为 `evidence/header-feedback-results.json`、`header-feedback-errors.txt`、`header-feedback-*.png` 和 `state-header-feedback-*-axe.json`。4 次 axe 审计无 violation；4 条 color-contrast incomplete 涉及背景文字／导航控件和伪元素，截图已复核，未发现提示区域对比度问题。

证据：`evidence/prototype-controls-results.json`、`prototype-controls-errors.txt`、`prototype-controls-*.png`、`state-prototype-controls-*-axe.json`、`wallet-boundary-*.png`、`state-wallet-boundary-*-axe.json`。axe 的 4 条 incomplete 涉及背景首页眉题与地平线文字的遮挡／伪元素，不涉及设置内控件；截图已复核，无新增可见对比度问题。

模型 74 条、词典 4 条、65 个 token 解析通过。本专项不替代完整 BDD 基线；原有全量浏览器脚本已迁移到新的场景入口。

链上记录专项 24 条通过，覆盖展开／收起、键盘焦点、完整 64 位 TxID 剪贴板校验、复制失败、已确认／待确认、卡片拖动与中英×浅深的 320/1440px 布局。4 次折叠区 axe 审计无 violation 和 incomplete。证据：`evidence/chain-disclosure-results.json`、`chain-disclosure-errors.txt`、`chain-disclosure-*.png`、`chain-disclosure-*-axe.json`。
